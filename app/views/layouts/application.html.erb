<link rel="stylesheet" href="flowershop.css">
<!-- <style >
  .card {
      /* Add shadows to create the "card" effect */

      box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
      transition: 0.3s;
      border-radius: 5px; /* 5px rounded corners */
      display:inline-block;
      background-color: rgb(0, 138, 207);
      position: fixed;
      z-index: 1;
      top:0;
      left: 0;
      overflow-x: hidden;
      padding-top:20px;
      border-right: 1px solid #000;
      width: fit-content;
      margin: 10px;
      border: 3px solid #73AD21;
      padding: 10px;
      float: left;
      /* display: flex; */
      height: 100%;
      justify-content: space-between;
  }
</style> -->

<style>
.card {
    /* Add shadows to create the "card" effect */

    box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
    transition: 0.3s;
    border-radius: 5px; /* 5px rounded corners */
    display:inline-block;
    background-color: rgb(0, 138, 207);

    width: fit-content;
    margin: 10px;
    border: 3px solid #73AD21;
    padding: 10px;
    float: left;
    /* display: flex; */
    justify-content: space-between;
}
</style>

<!DOCTYPE html>
<html>
  <head>
    <title>Neoplants</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <style>
      a { color: #FFFFF0; } /* CSS link color */
      body {text-shadow: 0 0 3px #FF0000, 0 0 5px #0000FF;}
    </style>

<!-- Disabled music for now -->
    <!-- <audio src="/app/assets/images/earthbound_2.mp3" autoplay loop>
      <p>Your browser does not support HTML5 audio playback</p>
    </audio> -->

  </head>

  <body>
    <%= yield %>
    <% if logged_in? %>
       <!-- need to make this not appear on /plants -->
      <!-- <%= link_to "See All Plants", plants_path %> -->
      <div class="card" >
        <h1>Info</h1>
        <p>Water Price: $1</p>
        <p>Soil Price: $2</p>
        <p>My Current Balance: $<%= current_user.balance  %></p>
        <% @plant = current_user.plants.sample %>
        <% action = Action.find_by(name:"Get $$$$") %>
        <!-- <% if @plant != nil %>
          <%= button_to "Get $$$$!", "/plants/#{@plant.id}/actions/#{action.id}"  %><br>
        <% end %> -->
        <%= button_to "My Flower Shop", plants_path, :method =>"get" %><br>
        <%= button_to "Buy New Plant!", new_plant_path, :method =>"get" %><br>
        <%= button_to "    Logout    ", logout_path, method: :delete %>
      </div>

      <!--  need to make this not appear on /plants/new-->
      <!-- <%= link_to "Add Plant to Your Collection", new_plant_path %> -->


    <% end %>


    <div class="logoutPlace">
    <% if logged_in? %>
      <!-- <%= button_to "Logout", logout_path, method: :delete %> -->
      <% else %>
    <% end %>
    </div>
    <!-- <%= link_to "Back to Homepage", root_path %> -->
  </body>
</html>
